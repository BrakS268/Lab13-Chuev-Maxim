# PROMPT LOG — Лабораторная работа №13

## Задание 1 (средняя): Определение агентов и их ролей

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты сеньор python разработчик. Предметная область — Система управления гостиницей (Заселение/выселение, Уборка комнат, Обработка запросов гостей, Биллинг). Задание 1: выделить типы агентов (не менее 3) и описать их: роль, входные данные, выходные данные, бизнес-правила. Агенты на Go, оркестратор на Python, брокер NATS.

**Что сделано:**
1. Создан `AGENTS.md` — описание 4 агентов системы:
   - `CheckInAgent` — заселение/выселение гостей (целевой агент)
   - `CleaningAgent` — уборка номеров (реактивный агент)
   - `GuestRequestAgent` — обработка запросов гостей (реактивный агент)
   - `BillingAgent` — биллинг и счета (BDI-агент)
2. Для каждого агента описаны: роль, топик NATS, входной/выходной JSON, бизнес-правила
3. Добавлена схема взаимодействия агентов и классификация по типам MAS

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 2 (средняя): Разработка прототипа агента на Go

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 2 — реализовать одного из агентов на Go, который подписывается на очередь NATS и обрабатывает задачи своего типа. Агент должен принимать структурированные данные (JSON), выполнять простую логику обработки и публиковать результат. Предметная область — гостиница.

**Что сделано:**
1. Создан `agents/checkin/main.go` — CheckInAgent на Go:
   - Подписка на топик `hotel.checkin`
   - Обработка `check_in`: валидация номера, проверка занятости, заселение
   - Обработка `check_out`: выселение + автоматическая отправка задачи уборки в `hotel.cleaning`
   - Публикация результатов в `hotel.results`
   - Повторные попытки подключения к NATS (5 попыток с паузой 2 сек)
   - Переменная окружения `NATS_URL` для настройки адреса
2. Создан `agents/checkin/go.mod` — Go-модуль с зависимостью nats.go v1.37.0

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~7 мин

---

## Задание 3 (средняя): Разработка оркестратора на Python

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 3 — создать оркестратор на Python с использованием asyncio и nats-py. Оркестратор должен уметь отправлять задачи агентам, ожидать результаты и обрабатывать таймауты. Предметная область — гостиница, вариант 29.

**Что сделано:**
1. Создан `orchestrator/orchestrator.py` — класс HotelOrchestrator:
   - Подключение к NATS с адресом из переменной окружения `NATS_URL`
   - Универсальный метод `send_task(subject, payload, timeout)` — отправка задачи и ожидание результата через asyncio.Future
   - Обработка таймаута через `asyncio.wait_for`
   - Удобные методы: `check_in`, `check_out`, `clean_room`, `guest_request`, `add_charge`
   - Подписка на `hotel.results` для получения ответов от агентов
   - Демонстрационный сценарий в `main()`
2. Создан `orchestrator/requirements.txt` — зависимость nats-py

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~7 мин

---
