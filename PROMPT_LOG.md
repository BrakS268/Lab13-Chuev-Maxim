# PROMPT LOG — Лабораторная работа №13

## Задание 1 (средняя): Определение агентов и их ролей

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты сеньор python разработчик. Предметная область — Система управления гостиницей (Заселение/выселение, Уборка комнат, Обработка запросов гостей, Биллинг). Задание 1: выделить типы агентов (не менее 3) и описать их: роль, входные данные, выходные данные, бизнес-правила. Агенты на Go, оркестратор на Python, брокер NATS.

**Что сделано:**
1. Создан `AGENTS.md` — описание 4 агентов системы:
   - `CheckInAgent` — заселение/выселение гостей (целевой агент)
   - `CleaningAgent` — уборка номеров (реактивный агент)
   - `GuestRequestAgent` — обработка запросов гостей (реактивный агент)
   - `BillingAgent` — биллинг и счета (BDI-агент)
2. Для каждого агента описаны: роль, топик NATS, входной/выходной JSON, бизнес-правила
3. Добавлена схема взаимодействия агентов и классификация по типам MAS

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 2 (средняя): Разработка прототипа агента на Go

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 2 — реализовать одного из агентов на Go, который подписывается на очередь NATS и обрабатывает задачи своего типа. Агент должен принимать структурированные данные (JSON), выполнять простую логику обработки и публиковать результат. Предметная область — гостиница.

**Что сделано:**
1. Создан `agents/checkin/main.go` — CheckInAgent на Go:
   - Подписка на топик `hotel.checkin`
   - Обработка `check_in`: валидация номера, проверка занятости, заселение
   - Обработка `check_out`: выселение + автоматическая отправка задачи уборки в `hotel.cleaning`
   - Публикация результатов в `hotel.results`
   - Повторные попытки подключения к NATS (5 попыток с паузой 2 сек)
   - Переменная окружения `NATS_URL` для настройки адреса
2. Создан `agents/checkin/go.mod` — Go-модуль с зависимостью nats.go v1.37.0

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~7 мин

---

## Задание 3 (средняя): Разработка оркестратора на Python

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 3 — создать оркестратор на Python с использованием asyncio и nats-py. Оркестратор должен уметь отправлять задачи агентам, ожидать результаты и обрабатывать таймауты. Предметная область — гостиница, вариант 29.

**Что сделано:**
1. Создан `orchestrator/orchestrator.py` — класс HotelOrchestrator:
   - Подключение к NATS с адресом из переменной окружения `NATS_URL`
   - Универсальный метод `send_task(subject, payload, timeout)` — отправка задачи и ожидание результата через asyncio.Future
   - Обработка таймаута через `asyncio.wait_for`
   - Удобные методы: `check_in`, `check_out`, `clean_room`, `guest_request`, `add_charge`
   - Подписка на `hotel.results` для получения ответов от агентов
   - Демонстрационный сценарий в `main()`
2. Создан `orchestrator/requirements.txt` — зависимость nats-py

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~7 мин

---

## Задание 4 (средняя): Настройка коммуникации через NATS

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 4 — развернуть NATS в Docker (docker-compose). Настроить взаимодействие между оркестратором и агентом, убедиться, что сообщения доставляются корректно.

**Что сделано:**
1. Создан `docker-compose.yml` — три сервиса:
   - `nats` — брокер NATS 2.10 с healthcheck на /healthz, порты 4222 и 8222 (мониторинг)
   - `checkin-agent` — Go-агент, зависит от healthy nats
   - `orchestrator` — Python-оркестратор, зависит от healthy nats
2. Создан `agents/checkin/Dockerfile` — двухэтапная сборка (builder + alpine)
3. Создан `orchestrator/Dockerfile` — python:3.12-slim
4. Создан `.env.example` — шаблон переменных окружения

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~15 мин

---

## Задание 5 (средняя): Логирование и мониторинг

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 5 — добавить логирование в оркестратор и агента (уровни INFO, ERROR). Организовать вывод логов в консоль и файл. Добавить простую метрику (счётчик обработанных задач).

**Что сделано:**
1. Создан `orchestrator/logger.py` — setup_logger(): запись в консоль (INFO+) и RotatingFileHandler в файл (DEBUG+), формат с timestamp и именем логгера
2. Создан `orchestrator/metrics.py` — класс Metrics: счётчики tasks_sent/succeeded/failed/timed_out, разбивка по топикам, метод summary()
3. Обновлён `orchestrator/orchestrator.py` — подключены logger и metrics, каждое событие логируется с нужным уровнем
4. Обновлён `agents/checkin/main.go` — setupLogger() пишет в консоль и файл через io.MultiWriter, atomic-счётчик обработанных задач
5. Обновлён `docker-compose.yml` — переменная LOG_DIR=/logs и общий named volume logs для всех сервисов

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~8 мин

---

## Задание 6 (средняя): Обработка ошибок и таймаутов

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 6 — реализовать в оркестраторе механизм повторной отправки задачи при сбое агента (retry, не более 3 раз). Добавить таймаут ожидания результата.

**Что сделано:**
1. Обновлён `orchestrator/orchestrator.py` — retry-цикл в `send_task`:
   - Константы MAX_RETRIES=3, RETRY_DELAY=2 сек
   - При таймауте логируется попытка, пауза RETRY_DELAY, следующая итерация
   - После MAX_RETRIES попыток бросается TimeoutError с понятным сообщением
2. Обновлён `orchestrator/metrics.py` — добавлен счётчик tasks_retried и метод record_retry(), выводится в summary()

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 7 (средняя): Запуск нескольких агентов одного типа

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 7 — запустить 2–3 экземпляра одного и того же Go-агента. Продемонстрировать балансировку нагрузки: оркестратор отправляет задачи, они распределяются между агентами.

**Что сделано:**
1. Обновлён `agents/checkin/main.go` — QueueSubscribe вместо Subscribe:
   - `nc.QueueSubscribe("hotel.checkin", "checkin-workers", ...)` — NATS автоматически распределяет сообщения между всеми подписчиками одной queue group
   - Переменная окружения AGENT_ID для идентификации экземпляра в логах
   - Отдельный лог-файл для каждого экземпляра (checkin-agent-1.log и т.д.)
2. Обновлён `docker-compose.yml` — три сервиса checkin-agent-1/2/3 с разными AGENT_ID
3. Обновлён `orchestrator/orchestrator.py` — сценарий с 4 параллельными заселениями через asyncio.gather() для демонстрации балансировки

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~7 мин

---

## Задание 8 (средняя): Создание API для запуска задач

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 8 — разработать простой REST API на FastAPI (Python), который принимает запросы на выполнение задач (HTTP → оркестратор → агенты → ответ). Предметная область — гостиница, вариант 29.

**Что сделано:**
1. Создан `api/main.py` — FastAPI приложение:
   - lifespan: подключение/отключение оркестратора при старте/остановке
   - `POST /checkin` — заселение гостя
   - `POST /checkout` — выселение гостя
   - `POST /cleaning` — уборка номера
   - `POST /requests` — запрос гостя (room_service, towels и т.д.)
   - `POST /billing/charge` — добавить списание
   - `GET /health` — проверка состояния
   - Pydantic-валидация входных данных, HTTP 400 при бизнес-ошибке, HTTP 504 при таймауте
2. Создан `api/requirements.txt` — fastapi, uvicorn, pydantic, nats-py
3. Создан `api/Dockerfile` — сборка из корня проекта, копирует файлы оркестратора
4. Обновлён `docker-compose.yml` — добавлен сервис api на порту 8000

**Что исправлялось вручную:** —

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 9 (средняя): Тестирование системы

**Инструмент:** Claude (claude.ai)

**Промпт:**
Задание 9 — написать модульные тесты для агента на Go (пакет testing) и для оркестратора на Python (pytest с моками NATS).

**Что сделано:**
1. Обновлён `agents/checkin/main.go` — добавлена функция logf(logger, ...) с проверкой на nil, все logger.Printf заменены на logf(), в handleCheckOut добавлена проверка nc != nil перед публикацией в NATS
2. Создан `agents/checkin/main_test.go` — 7 Go-тестов: успешное заселение, занятый номер, несуществующий номер, нулевые ночи, успешное выселение, выселение из свободного номера, itoa
3. Создан `tests/test_orchestrator.py` — 5 pytest-тестов с AsyncMock для NATS: успешный check_in, неуспешный check_in, check_out, таймаут с retry (3 попытки), счётчики метрик
4. Создан `tests/pytest.ini` — asyncio_mode = auto
5. Создан `tests/requirements-test.txt` — pytest, pytest-asyncio, nats-py

**Что исправлялось вручную:** удалён `logger_helper.go` — logf() была продублирована в двух файлах, оставлена только в main.go

### Итого
- Количество промптов: 2
- Время: ~12 мин

---